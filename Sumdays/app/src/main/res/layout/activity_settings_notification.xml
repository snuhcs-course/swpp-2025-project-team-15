<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/setting_notification_root"
    android:background="@color/background">

    <include
        android:id="@+id/header"
        layout="@layout/include_settings_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- ℹ️ ScrollView 안에는 다시 ConstraintLayout 넣기 (원본 유지) -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/masterSwitchLabel"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="데일리 메모 알림"
                android:textSize="@dimen/text_size_M"
                android:layout_marginTop="32dp"
                android:layout_marginHorizontal="20dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/masterSwitch" />

            <Switch
                android:id="@+id/masterSwitch"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checked="false"
                android:layout_marginHorizontal="20dp"
                app:layout_constraintTop_toTopOf="@id/masterSwitchLabel"
                app:layout_constraintBottom_toBottomOf="@id/masterSwitchLabel"
                app:layout_constraintEnd_toEndOf="parent" />

            <View
                android:id="@+id/separator1"
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/dark_divider"
                android:layout_marginTop="16dp"
                android:layout_marginHorizontal="20dp"
                app:layout_constraintTop_toBottomOf="@id/masterSwitchLabel" />

            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/alarmListContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="20dp"
                app:layout_constraintTop_toBottomOf="@id/separator1">

                <TextView
                    android:id="@+id/timeListTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="설정된 알림 시간 (최대 10개)"
                    android:textColor="@color/text_color_hint"
                    android:textSize="@dimen/text_size_S"
                    android:layout_marginTop="24dp"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintStart_toStartOf="parent" />

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/alarmTimeRecyclerView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="100dp"
                    tools:itemCount="3"
                    tools:listitem="@layout/item_alarm_time"
                    android:layout_marginTop="12dp"
                    app:layout_constraintTop_toBottomOf="@id/timeListTitle"
                    app:layout_constraintStart_toStartOf="parent" />

                <Button
                    android:id="@+id/addAlarmButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="새 알림 추가"
                    app:backgroundTint="@color/btn_violet"
                    android:layout_marginTop="24dp"
                    app:layout_constraintTop_toBottomOf="@id/alarmTimeRecyclerView"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent" />

            </androidx.constraintlayout.widget.ConstraintLayout>

            <Space
                android:layout_width="match_parent"
                android:layout_height="50dp"
                app:layout_constraintTop_toBottomOf="@id/alarmListContainer"/>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>